spring.application.name=backend

# Configuração da BD (PostgreSQL no Azure)
spring.datasource.url=jdbc:postgresql://athletrack.postgres.database.azure.com:5432/athletrack?sslmode=require
spring.datasource.username=luisassis
spring.datasource.password=dusCrias4

# Configurações JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Porta do backend
server.port=8080

