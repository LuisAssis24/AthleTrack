package estga.dadm.athletrack.screens.home

import androidx.compose.foundation.layout.*
import androidx.compose.material3.*
import androidx.compose.runtime.Composable

@Composable
fun HomeScreenProfessor(userName: String) {
    // Implementação da tela inicial para o professor
    // Aqui você pode adicionar os componentes necessários, como título, menu lateral, etc.
    // Utilize componentes como Column, Row, Text, Icon, etc. para criar a interface desejada.
    // Exemplo:
    Column {
        Text(text = "Bem-vindo, $userName")
        // Adicione mais componentes conforme necessário
    }
}